# Backend
PORT=3001
AGENT_TIMEOUT=600000

# API Key (proxy + Claude Code SDK 共用，不影响本机 CLI)
ANTHROPIC_API_KEY=your_api_key_here

# GitHub (optional, for github MCP server -- public repos work without it)
GITHUB_TOKEN=
